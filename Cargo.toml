[package]
name = "uroborosql-fmt"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tree-sitter = "0.19.5"

# crate ioのtree-sitter-sqlでは失敗するケースがあるため、githubから直接ダウンロードする必要がある
# path = "..."にはダウンロードしたtree-sitter-sqlのパスを入れる
tree-sitter-sql = { path = "C:\\Users\\saitoI063\\Work\\tree-sitter-sql" }